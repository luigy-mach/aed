#ifndef VECTORX_H
#define VECTORX_H
#include "nodo.h"
template<class T>
class Vectorx
{
private:
    Nodo<T> * m_pHead;
    Nodo<T> * m_pLast;
public:
    Vectorx();
    void Push(T D);
    void Pop(T &D);
    bool isEmpty();
};

#endif // VECTORX_H
